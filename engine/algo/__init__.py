"""Contract 1 — algorithm signatures. Published hour 2, frozen thereafter.

Owner: D. Every function here ships with a naive stub so that B and C can build
against it from hour 2 and never wait again. Stubs are upgraded in place; the
signatures do not move without announcement.
"""
from engine.algo import auction, forecast, llm, powerflow, reshape_lp, risk, thermal  # noqa: F401
